/**
 * Habilita/desabilita a simulação de rota (dev + produção).
 * Quando true, o botão "Simular rota" fica disponível no mapa
 * (inclusive em produção). Quando false, o app usa apenas GPS real.
 */
export const isSimulatingEnable = true;